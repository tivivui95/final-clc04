#include "Header.h"

int main() {
    Node* head = nullptr;
    Node* head2 = nullptr;
    fstream f;
    Navigation(choice);

   /* CreateSubject(head);
    Display(head);*/
    // AddAndDisplayStudent(head);
    // CreateSchoolYear(head);

    return 0;
}